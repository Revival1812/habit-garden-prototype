# Legacy Tree System

The old tree system is disabled in the current runtime.

This archive contains:

- old tree rendering scripts
- old tree layout and interaction styles
- old tree lab page
- old tree system docs
- old tree-related skill files
- old tree SVG asset directories

`index.html` and `detail.html` no longer reference the archived tree system files.
